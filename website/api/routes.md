---
title: API Routes
---

# API Routes

> Auto-generated by `docgen`. Do not edit directly.

## Health check endpoint

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| GET | `/api/health` | - | - | HealthCheck returns server health status. |

## Auth routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| POST | `/api/auth/register` | - | - | Register handles user registration |
| POST | `/api/auth/login` | - | - | Login handles user login |
| GET | `/api/auth/me` | Yes | - | Me returns the current user's profile |
| GET | `/api/auth/me/methods` | Yes | - | GetAuthMethods lists all auth methods for the current user |
| DELETE | `/api/auth/me/methods/{id}` | Yes | - | UnlinkAuthMethod removes an auth method from the current user |
| POST | `/api/auth/refresh` | - | - | Refresh handles token refresh using a refresh token |
| POST | `/api/auth/logout` | - | - | Logout revokes the current refresh token |
| POST | `/api/auth/logout-all` | Yes | - | LogoutAll revokes all refresh tokens for the current user |

## Google SSO routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| GET | `/api/auth/google/login` | - | - | Login initiates the Google OAuth flow |
| GET | `/api/auth/google/callback` | - | - | Callback handles the Google OAuth callback |
| POST | `/api/orgs/{id}/sso/google` | Yes | - | ConfigureSSO creates or updates Google SSO configuration for an organization |
| GET | `/api/orgs/{id}/sso/google` | Yes | - | GetSSOConfig returns the Google SSO configuration for an organization |

## Microsoft SSO routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| GET | `/api/auth/microsoft/login` | - | - | Login initiates the Microsoft OAuth flow |
| GET | `/api/auth/microsoft/callback` | - | - | Callback handles the Microsoft OAuth callback |
| POST | `/api/orgs/{id}/sso/microsoft` | Yes | - | ConfigureSSO creates or updates Microsoft SSO configuration for an organization |
| GET | `/api/orgs/{id}/sso/microsoft` | Yes | - | GetSSOConfig returns the Microsoft SSO configuration for an organization |

## Organization routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| POST | `/api/orgs` | Yes | - | Create creates a new organization |
| GET | `/api/orgs` | Yes | - | List returns all organizations the user belongs to |
| GET | `/api/orgs/{id}` | Yes | - | Get returns a specific organization |
| PUT | `/api/orgs/{id}` | Yes | - | Update updates an organization (admin only) |
| DELETE | `/api/orgs/{id}` | Yes | - | Delete deletes an organization (admin only) |
| POST | `/api/orgs/{id}/invitations` | Yes | - | CreateInvitation creates an invitation to join the organization (admin only) |
| POST | `/api/invitations/{token}/accept` | Yes | - | AcceptInvitation accepts an invitation and creates membership |
| GET | `/api/orgs/{id}/members` | Yes | - | ListMembers lists all members of an organization |
| PUT | `/api/orgs/{id}/members/{userId}/role` | Yes | - | UpdateMemberRole updates a member's role (admin only) |
| DELETE | `/api/orgs/{id}/members/{userId}` | Yes | - | RemoveMember removes a member from the organization (admin only) |
| PUT | `/api/orgs/{id}/branding` | Yes | - | UpdateBranding updates organization branding (admin only) |

## Audit log routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| GET | `/api/orgs/{id}/audit-log` | Yes | - | ListAuditLog handles GET /api/orgs/{id}/audit-log |
| GET | `/api/orgs/{id}/audit-log/export` | Yes | - | ExportAuditLog handles GET /api/orgs/{id}/audit-log/export |

## SSO role mapping routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| GET | `/api/orgs/{id}/sso/{provider}/role-mappings` | Yes | - | ListMappings handles GET /api/orgs/{id}/sso/{provider}/role-mappings |
| POST | `/api/orgs/{id}/sso/{provider}/role-mappings` | Yes | - | CreateMapping handles POST /api/orgs/{id}/sso/{provider}/role-mappings |
| DELETE | `/api/orgs/{id}/sso/{provider}/role-mappings/{mappingId}` | Yes | - | DeleteMapping handles DELETE /api/orgs/{id}/sso/{provider}/role-mappings/{mappingId} |

## Okta SSO routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| GET | `/api/auth/okta/login` | - | - | Login initiates the Okta OAuth/OIDC flow. |
| GET | `/api/auth/okta/callback` | - | - | Callback handles the Okta OAuth/OIDC callback. |
| POST | `/api/orgs/{id}/sso/okta` | Yes | - | ConfigureSSO creates or updates Okta SSO configuration for an organization. |
| GET | `/api/orgs/{id}/sso/okta` | Yes | - | GetSSOConfig returns the Okta SSO configuration for an organization. |
| POST | `/api/orgs/{id}/sso/okta/test` | Yes | - | TestConnection tests the Okta OIDC discovery for the configured domain. |
| GET | `/api/orgs/{slug}/sso/providers` | - | - | ListProviders returns the enabled SSO providers for an org identified by slug. |

## User group routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| POST | `/api/orgs/{id}/groups` | Yes | - | Create creates a new user group in the specified organization. Requires admin role. |
| GET | `/api/orgs/{id}/groups` | Yes | - | List returns all user groups in the specified organization. |
| PUT | `/api/orgs/{id}/groups/{groupId}` | Yes | - | Update modifies a user group's name or description. Requires admin role. |
| DELETE | `/api/orgs/{id}/groups/{groupId}` | Yes | - | Delete removes a user group from the organization. Requires admin role. |
| GET | `/api/orgs/{id}/groups/{groupId}/members` | Yes | - | ListMembers returns all members of a user group. |
| POST | `/api/orgs/{id}/groups/{groupId}/members` | Yes | - | AddMember adds a user to a group. Requires admin role. |
| DELETE | `/api/orgs/{id}/groups/{groupId}/members/{userId}` | Yes | - | RemoveMember removes a user from a group. Requires admin role. |

## Dashboard routes (org-scoped for list/create, dashboard ID for get/update/delete)

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| POST | `/api/orgs/{orgId}/dashboards` | Yes | - | Create creates a new dashboard in the specified organization. Requires admin or editor role. |
| GET | `/api/orgs/{orgId}/dashboards` | Yes | - | List returns all dashboards the user has view access to in the specified organization. |
| GET | `/api/dashboards/{id}` | Yes | - | Get returns a single dashboard by ID. |
| PUT | `/api/dashboards/{id}` | Yes | - | Update modifies a dashboard's title, description, or folder assignment. |
| DELETE | `/api/dashboards/{id}` | Yes | - | Delete removes a dashboard and its associated panels. |
| GET | `/api/dashboards/{id}/export` | Yes | - | Export serializes a dashboard and its panels to a portable JSON or YAML document. |
| POST | `/api/orgs/{orgId}/dashboards/import` | Yes | - | Import creates a dashboard and its panels from a JSON or YAML document. Requires admin or editor role. |

## Folder routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| POST | `/api/orgs/{orgId}/folders` | Yes | - | Create creates a new folder in the specified organization. |
| GET | `/api/orgs/{orgId}/folders` | Yes | - | List returns all folders the user has view access to in the specified organization. |
| GET | `/api/folders/{id}` | Yes | - | Get returns a single folder by ID. |
| PUT | `/api/folders/{id}` | Yes | - | Update modifies a folder's name, parent, or sort order. |
| DELETE | `/api/folders/{id}` | Yes | - | Delete removes a folder by ID. |

## Resource permission routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| GET | `/api/folders/{id}/permissions` | Yes | - | ListFolderPermissions returns the permission entries for a folder. Requires admin role. |
| PUT | `/api/folders/{id}/permissions` | Yes | - | ReplaceFolderPermissions replaces all permission entries for a folder. Requires admin role. |
| GET | `/api/dashboards/{id}/permissions` | Yes | - | ListDashboardPermissions returns the permission entries for a dashboard. Requires admin role. |
| PUT | `/api/dashboards/{id}/permissions` | Yes | - | ReplaceDashboardPermissions replaces all permission entries for a dashboard. Requires admin role. |

## Panel routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| POST | `/api/dashboards/{id}/panels` | Yes | - | Create adds a new panel to a dashboard. |
| GET | `/api/dashboards/{id}/panels` | Yes | - | ListByDashboard returns all panels belonging to a dashboard. |
| PUT | `/api/panels/{id}` | Yes | - | Update modifies a panel's title, type, layout, or query configuration. |
| DELETE | `/api/panels/{id}` | Yes | - | Delete removes a panel from its dashboard. |

## Prometheus data source routes (legacy, backwards compatible)

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| GET | `/api/datasources/prometheus/query` | - | - | Query executes a PromQL range query |
| GET | `/api/datasources/prometheus/metrics` | - | - | Metrics returns all available metric names (GET /api/datasources/prometheus/metrics) |
| GET | `/api/datasources/prometheus/labels` | - | - | Labels returns all available label names (GET /api/datasources/prometheus/labels) |
| GET | `/api/datasources/prometheus/label/{name}/values` | - | - | LabelValues returns all values for a specific label (GET /api/datasources/prometheus/label/{name}/values) |

## Multi-source datasource routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| POST | `/api/orgs/{orgId}/datasources/test` | Yes | - | TestConnectionDraft tests datasource connectivity against unsaved configuration. |
| POST | `/api/orgs/{orgId}/datasources` | Yes | - | Create creates a new datasource for an organization |
| GET | `/api/orgs/{orgId}/datasources` | Yes | - | List lists all datasources for an organization |
| GET | `/api/orgs/{orgId}/datasources/{dsId}/trace-datasources` | Yes | - | ListTraceDatasources returns tracing datasources in the same org for linking |
| GET | `/api/datasources/{id}` | Yes | - | Get returns a single datasource |
| PUT | `/api/datasources/{id}` | Yes | - | Update updates a datasource |
| DELETE | `/api/datasources/{id}` | Yes | - | Delete deletes a datasource |
| GET | `/api/datasources/{id}/labels` | Yes | - | Labels returns indexed labels/fields for log datasources |
| GET | `/api/datasources/{id}/labels/{name}/values` | Yes | - | LabelValues returns indexed values for a specific log datasource field |
| GET | `/api/datasources/{id}/metric-names` | Yes | - | MetricNames returns metric names for Prometheus/VictoriaMetrics datasources |
| POST | `/api/datasources/{id}/query` | Yes | - | Query executes a query against a datasource |
| GET | `/api/datasources/{id}/traces/{traceId}` | Yes | - | GetTrace returns a trace by id from a tracing datasource. |
| GET | `/api/datasources/{id}/traces/{traceId}/service-graph` | Yes | - | TraceServiceGraph returns a service dependency graph for a trace. |
| POST | `/api/datasources/{id}/traces/search` | Yes | - | SearchTraces searches traces on a tracing datasource. |
| GET | `/api/datasources/{id}/traces/services` | Yes | - | TraceServices lists available services from a tracing datasource. |
| POST | `/api/datasources/{id}/stream` | Yes | - | Stream opens a live log stream against a datasource |
| POST | `/api/datasources/{id}/test` | Yes | - | TestConnection tests datasource connectivity and auth configuration. |

## VMAlert proxy routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| GET | `/api/datasources/{id}/vmalert/alerts` | Yes | - | Alerts proxies GET /api/datasources/{id}/vmalert/alerts to VMAlert. |
| GET | `/api/datasources/{id}/vmalert/groups` | Yes | - | Groups proxies GET /api/datasources/{id}/vmalert/groups to VMAlert. |
| GET | `/api/datasources/{id}/vmalert/rules` | Yes | - | Rules proxies GET /api/datasources/{id}/vmalert/rules to VMAlert. |
| GET | `/api/datasources/{id}/vmalert/health` | Yes | - | Health proxies GET /api/datasources/{id}/vmalert/health to VMAlert. |

## AlertManager proxy routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| GET | `/api/datasources/{id}/alertmanager/alerts` | Yes | - | ListAlerts proxies GET /api/datasources/{id}/alertmanager/alerts to AlertManager. |
| GET | `/api/datasources/{id}/alertmanager/silences` | Yes | - | ListSilences proxies GET /api/datasources/{id}/alertmanager/silences to AlertManager. |
| POST | `/api/datasources/{id}/alertmanager/silences` | Yes | - | CreateSilence proxies POST /api/datasources/{id}/alertmanager/silences to AlertManager. |
| DELETE | `/api/datasources/{id}/alertmanager/silences/{silenceId}` | Yes | - | ExpireSilence proxies DELETE /api/datasources/{id}/alertmanager/silences/{silenceId} to AlertManager. |
| GET | `/api/datasources/{id}/alertmanager/receivers` | Yes | - | ListReceivers proxies GET /api/datasources/{id}/alertmanager/receivers to AlertManager. |
| GET | `/api/datasources/{id}/alertmanager/health` | Yes | - | Health proxies GET /api/datasources/{id}/alertmanager/health to AlertManager. |

## GitHub Copilot auth routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| POST | `/api/auth/github/device` | Yes | - | StartDeviceFlow initiates the GitHub device flow for Copilot authentication. |
| POST | `/api/auth/github/device/poll` | Yes | - | PollDeviceFlow polls GitHub for device flow completion and saves the connection. |
| GET | `/api/auth/github/connection` | Yes | - | GetConnection returns the GitHub connection status for the current user. |
| DELETE | `/api/auth/github/connection` | Yes | - | Disconnect removes the GitHub connection for the current user. |

## AI provider routes (org-scoped)

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| GET | `/api/orgs/{id}/ai/providers` | Yes | Yes | ListProviders returns the enabled AI providers for the current organization. |
| GET | `/api/orgs/{id}/ai/models` | Yes | Yes | ListModels returns available models for a given AI provider. |
| POST | `/api/orgs/{id}/ai/chat` | Yes | Yes | Chat proxies a chat completion request to the resolved AI provider, with streaming support. |
| POST | `/api/orgs/{id}/ai/providers` | Yes | Yes | CreateProvider registers a new AI provider for the organization. Requires admin role. |
| PUT | `/api/orgs/{id}/ai/providers/{pid}` | Yes | Yes | UpdateProvider modifies an existing AI provider's configuration. Requires admin role. |
| DELETE | `/api/orgs/{id}/ai/providers/{pid}` | Yes | Yes | DeleteProvider removes an AI provider from the organization. Requires admin role. |
| POST | `/api/orgs/{id}/ai/providers/{pid}/test` | Yes | Yes | TestProvider verifies connectivity to an AI provider by listing its models. Requires admin role. |

## Dashboard variable routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| GET | `/api/dashboards/{id}/variables` | Yes | - | List returns all template variables for a dashboard. |
| POST | `/api/dashboards/{id}/variables` | Yes | - | BulkCreate creates or upserts multiple template variables for a dashboard. |
| PUT | `/api/variables/{varId}` | Yes | - | Update modifies a template variable's properties. |
| DELETE | `/api/variables/{varId}` | Yes | - | Delete removes a template variable from its dashboard. |

## Grafana conversion route

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| POST | `/api/convert/grafana` | Yes | - | Convert transforms a Grafana dashboard JSON into the Ace dashboard format. |

## Grafana auto-discovery routes

| Method | Path | Auth | Org Member | Description |
|--------|------|------|------------|-------------|
| POST | `/api/grafana/connect` | Yes | - | Connect tests connectivity to a Grafana instance and returns its version. |
| GET | `/api/grafana/dashboards` | Yes | - | ListDashboards returns dashboard summaries from a remote Grafana instance. |
| GET | `/api/grafana/dashboards/{uid}` | Yes | - | GetDashboard fetches a single dashboard's full JSON from a remote Grafana instance by UID. |

